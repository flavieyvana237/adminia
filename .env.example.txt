# ======================
# Application & Domain
# ======================
DOMAIN_NAME=adminia.com
SITE_URL=http://127.0.0.1:8000
VERSION=1.0.0
USE_DOCKER=no

# ======================
# Django Configuration
# ======================
DJANGO_DEBUG=True
DJANGO_SECRET_KEY=change_me_in_production_secret_key_example
DJANGO_ALLOWED_HOSTS=127.0.0.1,localhost

# ======================
# Database Configuration (PostgreSQL)
# ======================
DB_NAME=adminia_db
DB_USER=adminia_user
DB_PASSWORD=your_db_password_here
DB_HOST=127.0.0.1
DB_PORT=5432

# URL de connexion à la base de données
DATABASE_URL=postgres://adminia_user:your_db_password_here@127.0.0.1:5432/adminia_db

# ======================
# Redis & Celery
# ======================
REDIS_URL=redis://127.0.0.1:6379/0
CELERY_BROKER_URL=redis://127.0.0.1:6379/0

# ======================
# Email Configuration
# ======================
# En local, utilise 'django.core.mail.backends.console.EmailBackend' pour voir les emails dans le terminal
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=your_email@example.com
EMAIL_HOST_PASSWORD=your_app_password_here